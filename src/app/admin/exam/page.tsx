
'use client';

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function ExamPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Exam</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Exam management page. This page is under construction.</p>
      </CardContent>
    </Card>
  );
}
