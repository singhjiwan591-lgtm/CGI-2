
'use client';

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function TransportPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Transport</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Transport management page. This page is under construction.</p>
      </CardContent>
    </Card>
  );
}
